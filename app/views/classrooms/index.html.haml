.content-header
  %nav= link_to t('text.new'), new_classroom_path, class: 'btn btn-primary-outline btn-sm'
  %h2= title

= display_when_present @classrooms do
  %table.table.table-striped
    %thead
      %tr
        %th= Classroom.human_attribute_name :title
        %th.actions

    %tbody= render @classrooms
