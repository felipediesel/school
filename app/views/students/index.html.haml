.content-header
  %nav
    .btn-group
      = button_tag t('text.filter'), type: "button", id: 'dropdown-stateses', class: 'btn btn-secondary btn-sm dropdown-toggle', aria: { expanded: "false", haspopup: "true"}, "data-toggle" => "dropdown"
      .dropdown-menu{"aria-labelledby" => "dropdown-stateses"}
        - Student::STATUSES.each do |status|
          = link_to Student.human_status(status), students_path(status: status), class: 'dropdown-item'
    = link_to t('text.new'), new_student_path, class: 'btn btn-primary-outline btn-sm'

  %h2= title

%table.table.table-hover
  %thead
    %tr
      %th.text-xs-center= Student.human_attribute_name :code
      %th= Student.human_attribute_name :name
      %th= Classroom.model_name.human count: 2
      %th= Student.human_attribute_name :status
  %tbody= render @students
