.content-header
  %nav= link_to t('text.new'), new_modality_path, class: 'btn btn-outline-primary btn-sm'
  %h2= title

= display_when_present @modalities do
  %table.table.table-hover
    %thead
      %tr
        %th= Modality.human_attribute_name :name
        %th
        %th

    %tbody= render @modalities
